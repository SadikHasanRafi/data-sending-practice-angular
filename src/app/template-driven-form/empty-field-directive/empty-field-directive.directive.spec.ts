import { EmptyFieldDirectiveDirective } from './empty-field-directive.directive';

describe('EmptyFieldDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyFieldDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
